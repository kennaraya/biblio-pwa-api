{
  "name": "biblio-pwa-api",
  "version": "1.0.0",
  "private": true,
  "description": "PWA de biblioteca con API (Express) en el mismo origen",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
